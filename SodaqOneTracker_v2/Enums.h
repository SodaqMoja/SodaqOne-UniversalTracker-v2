#ifndef _ENUMS_h
#define _ENUMS_h

enum LoraInitResult {
    LORA_INIT_FAIL = 0,
    LORA_INIT_FULL,
    LORA_INIT_DEVICE_ONLY
};

enum LedColor {
    NONE = 0,
    RED,
    GREEN,
    BLUE
};

#endif
